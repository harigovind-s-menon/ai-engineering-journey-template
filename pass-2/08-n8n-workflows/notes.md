# Low-Code GenAI Workflows with n8n

Your notes and exported workflow JSON for this lesson live here.

Mark it done from the repo root when finished:

    touch <this-folder>/DONE.md && git add -A && git commit -m "done: n8n-workflows" && git push

> Placeholder — do NOT create DONE.md until the lesson is actually complete.